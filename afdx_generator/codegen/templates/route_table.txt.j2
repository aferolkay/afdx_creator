{{ c }}** Generated by afdx_generator -- do not edit by hand.
{{ c }}** Virtual-link routing table for switch: {{ switch_label }}
{{ c }}**
{{ c }}** Ports on this switch:
{% for port, peer in port_map %}
{{ c }}**   port {{ port }} -> {{ peer }}
{% endfor %}
{{ c }}**
{{ c }}** Entries (a link that only passes through still needs an entry here; the router aborts
{{ c }}** the run on an id it cannot find). Several ports on one line means multicast.
{% for entry in entries %}
{{ c }}**   {{ entry.key }} = {{ entry.label }} -> port(s) {{ entry.ports | join(", ") }}
{% endfor %}
{{ c }}**
{{ c }}** NOTE: no comments below this point. The table parser mis-handles a comma appearing
{{ c }}** after a port list, so annotations stay in this header block.
{% for entry in entries %}
{{ entry.key }}{{ sep }}{{ open_brace }}{{ entry.ports | join(port_sep) }}{{ close_brace }}
{% endfor %}
